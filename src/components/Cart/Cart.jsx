import React from 'react'

const Cart = ({cartItems, handleDeleteFromCart}) => {
  return (
    <div className='container mt-4'>
      <h2>Your Cart</h2>
      {cartItems.length === 0 ? }

    </div>
  )
}

export default Cart